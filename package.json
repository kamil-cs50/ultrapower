{
  "name": "ultrapower",
  "version": "1.0.0",
  "description": "## Struktura katalogów ``` assets/ └── images/     ├── products/      # Zdjęcia produktów     ├── categories/    # Zdjęcia kategorii     └── banners/       # Bannery i elementy główne ```",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html products/*.html cart/index.html --public-url ./"
  },
  "dependencies": {
    "three": "^0.174.0"
  },
  "devDependencies": {
    "parcel": "^2.13.3"
  }
}